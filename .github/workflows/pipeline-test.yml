name: "🧪 Pipeline Pruebas"

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 1. Descargar el código
      - name: "📥 Checkout código"
        uses: actions/checkout@v4

      # 2. Configurar Python
      - name: "🐍 Configurar Python 3.11"
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 3. Instalar dependencias
      - name: "📦 Instalar dependencias"
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4. Ejecutar pruebas
      - name: "🧪 Ejecutar pruebas"
        run: |
          pytest --cov=app --cov-report=term-missing --cov-report=xml -v

      # 5. Validar cobertura >= 60%
      - name: "📊 Validar cobertura mínima (≥60%)"
        run: |
          COVERAGE=$(python -c "
          import xml.etree.ElementTree as ET
          tree = ET.parse('coverage.xml')
          root = tree.getroot()
          rate = float(root.attrib['line-rate']) * 100
          print(f'{rate:.0f}')
          ")
          echo "Cobertura actual: ${COVERAGE}%"
          if [ "$COVERAGE" -lt 60 ]; then
            echo "❌ FALLO: Cobertura ${COVERAGE}% es menor al 60% requerido"
            exit 1
          fi
          echo "✅ Cobertura ${COVERAGE}% cumple el mínimo de 60%"

      - name: "🚀 Deploy a ambiente Pruebas"
        if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
        run: |
          echo "✅ Deploy a pruebas pendiente de configurar"